[package]
name = "finte"
version = "0.1.0"
edition = "2018"
description = "convert between integer and Rust enum"
license = "MIT"

[dependencies.finte-derive]
version = "0.1.0"
optional = true

[features]
default = [ "derive" ]
derive = [ "finte-derive" ]
